**Milestone 1: System Smoke Tests**

- [ ] `ChannelType` is defined with `API` and `UI` values
- [ ] Shop Smoke Test uses `@Channel` (no base class or API/UI subclasses)
- [ ] Smoke Tests run for API Channel
- [ ] Smoke Tests run for UI Channel


**Milestone 2: E2E Tests**

- [ ] Each E2E test uses `@Channel` (no base class or API/UI subclasses)
- [ ] At least one test method runs on both channels
- [ ] At least one test method runs on only one channel (if applicable)
- [ ] E2E Tests run for API Channel
- [ ] E2E Tests run for UI Channel
