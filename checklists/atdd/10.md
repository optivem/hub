**Milestone 1: Migrate to Stubs**

- [ ] `BaseAcceptanceTest` extends `BaseScenarioDslTest` and overrides `getFixedExternalSystemMode()` to return `ExternalSystemMode.STUB`
- [ ] All acceptance test classes extend `BaseAcceptanceTest`
- [ ] `PlaceOrderPositiveTest` and `PlaceOrderNegativeTest` exist with acceptance scenarios
- [ ] `ViewOrderPositiveTest` and `ViewOrderNegativeTest` exist with acceptance scenarios
- [ ] `CancelOrderPositiveTest` and `CancelOrderNegativeTest` exist with acceptance scenarios
- [ ] `PublishCouponPositiveTest` and `PublishCouponNegativeTest` exist with acceptance scenarios
- [ ] `BrowseCouponsPositiveTest` exists with acceptance scenarios


**Milestone 2: Extend with Stub-Only Scenarios**

- [ ] At least one scenario configures a Tax rate via `given().country().withTaxRate(...)` and asserts the exact rate
- [ ] At least one scenario verifies exact total price (subtotal + tax amount)


**Milestone 3: Suite Separation**

- [ ] Acceptance suite runs with `ExternalSystemMode.STUB`
- [ ] E2E suite runs with `ExternalSystemMode.REAL`
- [ ] Both suites pass independently
